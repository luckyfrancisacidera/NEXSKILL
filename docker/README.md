# Docker Configuration

This folder contains Docker-related configuration for the SkillSense monorepo.

## Structure

- `client` → React TypeScript app
- `server` → ASP.NET Core (Clean Architecture)
- `services` → Python resume parsing service

## Planned Files

- `Dockerfile.client`
- `Dockerfile.server`
- `Dockerfile.resume-service`
- `docker-compose.yml`
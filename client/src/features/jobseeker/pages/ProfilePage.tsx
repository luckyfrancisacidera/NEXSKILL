import { Card } from '@shared/components/Card';

export const ProfilePage = () => (
  <Card>
    <h2 className="text-2xl font-semibold">Profile</h2>
    <p className="mt-2 text-zinc-500">Manage your resume, summary, and public profile preferences.</p>
  </Card>
);
